# 项目进度记录：2026-09-13（日志模块收尾 + 事件总线完成）

> 用途：跨会话/跨 Agent 恢复上下文的项目记忆（docs/memory/）。
> 上期：`docs/memory/progress-2026-09-10.md`（设计收敛、R3 引入、日志 v1.1、池模块）
> 关联：`docs/architecture/event-bus-design.md`（**v0.5.1**）、`docs/architecture/logging-design.md`（**v1.5**）、`docs/architecture/pool-manager-design.md`（v0.2.8）、`docs/standards/agent-role.md`

---

## 1. 本期产出

| 产出 | 说明 |
|---|---|
| **事件总线设计稿（新建）** | `docs/architecture/event-bus-design.md`：v0.1 → **v0.5.1**。含契约、通道机制（copy-on-write 数组）、发布语义精确表、异常隔离、线程模型、生命周期三条注销路径、R3 桥、事件类型组织、§9 共 27 条验收、**附录 A**（与 CommunityToolkit.Mvvm messenger 的逐条对照取舍）、**附录 B**（为什么不做 `this` 自动装配） |
| **事件总线实现完成** | `Core/Events/`：`IZEventBus` / `ZEventBus` / `ZEventState` / `ZEventsState` / `EventChannel/{EventChannelBase, EventChannel<T>, Subscription<T>}` 共 7 文件；**验收 19/19 ALL PASS**（见 §3） |
| **日志模块收尾** | `[CallerFilePath]` 打通到输出；前缀改"两分支"；路径截断到项目相对路径；格式化提取为 `LogFormatter`（两 Sink 共用一份实现）；`logging-design.md` → v1.5 |
| **测试程序集骨架** | `Assets/Zipper/Tests/Zipper.Tests.asmdef`（现代写法 + `Zipper.Core` 引用 + `includePlatforms: ["Editor"]`）；**尚无测试文件** |
| **两轴代码审查** | 用 `code-review` skill 的两轴（规范轴 / 规格轴）并行子代理审事件总线，产出缺陷清单；随后逐轮复核至 ALL PASS |

## 2. 本期定案（事件总线，v0.5 / v0.5.1）

| 事项 | 结论 |
|---|---|
| 事件类型约束 | **必须引用类型**（`where T : class`）。原"不加约束、载荷优先 `readonly struct`"**作废**；代价：每次发布需一个事件对象（总线自身仍零分配）；**struct 事件不支持** |
| 状态查询 | **`ZEventsState GetState()`** 取代 `SubscriberCount<T>()`；DTO 照对象池（`ZPoolState`/`ZPoolsState`）形状：`Type EventType`、`{ get; private set; }`、`TotalSubscribers` 必须 `+=` 累加 |
| 命名统一 | `recipient` → **`subscriber`**；`UnregisterAll` → **`UnsubscribeAll`**（代码与文档同一个词） |
| 收件人为 null | `UnsubscribeAll(null)` → **记 Warning 并忽略**（不抛）：既防误清匿名订阅，又不给关闭期路径添异常 |
| 反向边（凭据→通道） | `Subscription` **持有所属通道**，Dispose 只标记、**摘除由通道完成**；通道必须提供 `Remove(Subscription<T>)`（按身份），否则 `Remove(this)` 会被绑到 `Remove(object)` 而**静默删不掉** |
| `Remove(...)` 契约 | 三个重载都返回**移除个数**（`int`），上层据此 `true` / "Warning + false" —— 避免**未命中却谎报成功** |
| 批量释放 | `EventChannel.Dispose()` 用"一次性 `Array.Empty` + 逐个只标记"（O(n)）；`Remove` 用"复制 + 替换引用"（`Array.Resize`/`Array.FindAll` 本质都是 copy-on-write） |
| 关闭期语义 | `_disposed` 门：Dispose 后 `Publish` no-op；`Subscribe` 返回**共享空凭据**（`EmptyIDisposable`），绝不返回 null |
| 主线程校验 | 总线自持线程 ID（ctor 捕获）；`Subscribe`/`Publish`/`Unsubscribe`/`UnsubscribeAll` 入口校验，违规记 Error 并忽略；**不复用日志的 dispatcher**（三条耦合理由见 §4.2） |
| 不采纳（对照 CommunityToolkit.Mvvm） | class 信封（`MessageBase`/`ValueChangedMessage<T>`）、弱引用订阅、请求-应答、MVVM 值变化语义、`IRecipient` 声明式注册、静态 `.Default` 单例 —— **一律不采纳**（逐条理由在附录 A） |
| 不做 `this` 自动装配 | 基类 / 扩展方法 receiver / 反射发现三条路全不做；留"扩展方法 + 空标记接口"作为将来 10 行可选项（附录 B） |
| 通道 token | **延后**（同一事件类型多通道），等真实需求 |

## 3. 验收证据（事件总线）

用临时 harness（Core 源码 + 测试 `Main` 编进同一 exe，直接跑真实 `ZEventBus`，含假 `IZLogger` 记录日志调用；产物在 `Library/` 内、跑完即删）：

```
ALL PASS（19 项）—— 关键项：
  状态计数 TotalSubscribers / 按类型计数 / EventType 是 Type ✓
  凭据 Dispose 真正摘除（计数 2→1）✓
  快照语义：回调内退订自己、回调内订阅同类型 ✓
  异常隔离：其余仍收到 + 记 Error ✓
  主线程校验：非主线程 Publish 不派发 + 记 Error；非主线程 Subscribe 返回非 null 空凭据 ✓
  Publish 无人订阅：不记任何日志 ✓
  Unsubscribe 三情形：命中 true / 未命中 false+Warning / 无该类型通道 false+Warning ✓
  Subscribe(null handler) 抛 ArgumentNullException ✓
  UnsubscribeAll(null) 不误清匿名；UnsubscribeAll(owner) 只清该收件人 ✓
  关闭期：Dispose 后不抛、Subscribe 被拒绝、GetState 为空、Dispose 幂等 ✓
  规模：5000 个订阅的通道 Dispose 耗时 0ms（O(n²) 那版会明显慢）✓
```

日志模块（前段）：`LogFormatter.Format` 与"提取前两个 Sink 的旧表达式"逐字对照 8 条用例 **ALL PASS**（含 nameof 分支、绝对/相对路径截断、非 Assets 退化文件名、null 路径）。

## 4. 我犯过并已更正的错误（方法论留痕）

| 错误 | 更正 |
|---|---|
| 断言"`R3.Unity` 未装" | **错**——只查了 NuGet 的 core DLL，没看 UPM 包。实际 `Packages/manifest.json` 里 `com.cysharp.r3` 已装（1.3.1，与 core 一致）。已更正 `event-bus-design.md` v0.2 |
| 建议"避免原地 `Array.Resize`" | **错**——`Array.Resize` 本就是"新数组 + 复制 + 替换字段"= copy-on-write。v0.5.1 撤回 |
| 文档写"两参重载 `subscriber` 为 null → 抛" | **错**——`subscriber = null` **就是匿名订阅**（单参重载正是这么调的），合法。v0.5.1 更正 |
| 验收观测项选错 | 曾用"恒为 0 的 `TotalSubscribers`"当证据判断 `UnsubscribeAll(null)` 是否误清；换成按类型计数后重测才成立。**教训：证据的观测项本身必须先验证** |
| 检查写弱导致假 PASS | "Dispose 后 Subscribe 应被拒绝"一条，曾把新凭据 `Dispose` 掉之后才计数 → 假 PASS；改成"拿到后不 Dispose 再计数"才暴露问题 |

## 5. 当前状态

- **文档版本**：event-bus-design **v0.5.1**、logging-design **v1.5**、pool-manager-design v0.2.8、resource-manager-design v0.3、core-design v0.9.3、bootstrap-design v1.0、roadmap v0.10（后三份仍待使用者审批）
- **代码**：`Core/{Boot,Events,Logging,Sink}`、`Pool/`、`Resources/`、`DI/` 均已落地；事件总线与日志模块**自测通过**
- **提交**：`c8ad22d`（docs: 事件总线设计稿 v0.5.1）、`802c838`（feat(core): 事件总线）、`7e7c0d0`（test(core): 测试程序集骨架）；工作区干净，**本地领先 origin 3 个提交、未 push**
- **协作口径**：本期多次按 `agent-role.md` §2 的**例外条款**（使用者明确要求 + 当次最小范围）由 AI 直接改代码：日志 `LogFormatter` 提取、事件总线 `Subscription` 反向边修复、`EmptyIDisposable` 修复等；规范文档未改动

## 6. 待办 / 下一步

- [ ] **事件总线 §9 的 27 条用例**：`Tests/` 目前只有 asmdef 骨架 → 落 `[Test]`（先 1–14）；假 `IZLogger` 放进该程序集共用；"零分配"两条用 `GC.GetAllocatedBytesForCurrentThread()` 取差断言
- [ ] **R3 桥**：`AsObservable<T>(this IZEventBus)`（放依赖 R3 的桥接程序集；`Observable.Create<T>(observer => bus.Subscribe<T>(e => observer.OnNext(e)))`，退订联动天然成立；不需要 `Synchronize`——总线仅主线程发布）
- [ ] **文档同步欠账**（唯一"文档与已定案不一致"处）：
  - `core-design.md` §4.3：把"后台线程发布经主线程泵"改为"**v1 仅主线程，跨线程由调用方 `SwitchToMainThread`**"，并指向 `event-bus-design.md`
  - `bootstrap-design.md`：`Events` 阶段是预留占位；日志装配顺序（dispatcher → sinks → router → driverObject → Attach）
  - `pool-manager-design.md`（校验前移、`Get*` vs `Clear/Destroy` 两套语义、`_disposed`、`Return` 假 null）
  - `resource-manager-design.md`（`[CallerMemberName]` 必须声明在接口上、句柄释放语义统一）
  - `technical-roadmap.md`（状态清单 + 文档清单补 `event-bus-design.md`）
- [ ] **待决项**（event-bus-design §13.2）：D2（`Publish` 返回值）、D5（桥的 `OnCompleted`）、D6（`Events` 阶段）、D7（事件类型两级归属）、D8（调试视图）、D9（`Zipper.Core.asmdef` 开 `overrideReferences` 做编译期强制）、D10（值类型 subscriber 调试期检查）、D11（`CancellationToken` 重载）、D12（扩展方法糖）、D13（dispatcher 中立化）
- [ ] 可选微优化：`EventChannel<T>.Remove(...)` 空数组早退（`Array.FindAll` 删 0 个也会分配）
- [ ] **Push**：本地领先 3 个提交，需使用者发话（连不上 github 多为加速器未开）
- [ ] 旧账：AGENTS.d/、C# 编码规范、roadmap/core-design/bootstrap 的审批

## 7. 变更记录

| 日期 | 说明 |
|---|---|
| 2026-09-13 | 初版：记录日志模块收尾（CallerFilePath + LogFormatter + 相对路径截断，logging-design v1.5）、事件总线从设计到实现完成（v0.5.1 + 19/19 验收通过）、测试程序集骨架、四项错误更正留痕、待办清单 |
