# 项目进度记录：2026-09-07 ~ 09-10（设计收敛与文档治理）

> 用途：跨会话/跨 Agent 恢复上下文的项目记忆（docs/memory/）。
> 上期：`docs/memory/progress-2026-09-05-06.md`（资源管理器设计 + 框架骨架 + 仓库建立）
> 关联：`docs/planning/technical-roadmap.md`（v0.7）、`docs/architecture/core-design.md`（v0.7）、`docs/architecture/resource-manager-design.md`（v0.3）

---

## 1. 本期产出

| 产出 | 说明 |
|---|---|
| **设计稿去实现化** | 两份设计稿删除实现级代码，统一改为**思路级伪代码**（commit `aae9729`，−823/+375 行）：API 定义改为"设计要素"表，流程改为"做法+顺序" |
| **新增规范 `docs/standards/agent-role.md`** | AI 只负责架构设计与思路级伪代码，**代码实现由使用者完成**（分工固化，跨会话生效） |
| **新增规范 `docs/standards/naming-convention.md`** | Z / IZ 前缀约定；**内部机制类型不加前缀**；判定口径"`public` ≠ 品牌 API" |
| **roadmap 更新 v0.4 → v0.7** | v0.4：现状盘点同步、键体系改 address/label、Core 定为零框架依赖例外、Core 基础设施一节、里程碑状态刷新、风险表；v0.7：事件总线改单实现 |
| **research 文档标注** | §7「UPM 包结构与内嵌开发做法」标注**已被 roadmap v0.3 的右键导出取代**，避免误导 |
| **进度记忆体系** | 建立 `docs/memory/`，本次为第二份记录 |

## 2. 决策演进（本期最重要）

### 2.1 事件总线：五步收敛

```
暂缓（等 R3）→ v0.4「最小版」自研类型化总线 → v0.5「双实现」（自研 + R3 封装）
  → v0.6 命名统一（IZEventBus / ZEventBus / ZR3EventBus）
  → v0.7（使用者修订）收敛为「单实现 + R3 桥」：IZEventBus + ZEventBus（Core 唯一实现）
       + R3 桥（AsObservable 扩展，依赖 R3 的程序集，不进 Core）
```

**收敛依据（v0.7，写在 core-design §4.3）**：R3 没有按类型路由的全局注册表（`Subject<T>` 是命名实例，仍要人工传递）；Rx `OnError` 会终止整条流，与总线"单订阅者抛异常 → 继续通知其余"冲突，故 R3 版总线必然复制一份自研内核；桥比第二实现更小更强且可事后补；R3 版放 Core 破地基分层、放 UI 层则依赖方向倒挂。

**最终纪律**：总线只做**路由**、R3 只做**加工**；同一件事只发布一次，需要节流/合并的一方在**订阅侧**（R3 桥）加工；UI 通知也走总线（不再分两条）。

**已知代价（写进文档）**：桥之后的 Observable 链上抛异常属 Rx 语义（使用方自理），总线侧仍以为订阅存在——实现 UI 时建议链尾统一兜底。

### 2.2 其它已定决策

| 主题 | 结论 |
|---|---|
| 键体系 | 枚举键方案**废弃** → 框架公共 API 只收 **address / label**；业务键清单归使用工程；解析器列后续演进 |
| 命名约定 | 对外概念/入口 `Z*`/`IZ*`；**内部机制不加前缀**（`AssetHandle`/`AssetHandleBase`/`PrefabAsset`） |
| Core 依赖 | **零框架依赖**（不引 VContainer/UniTask/Addressables/R3）——与旧版 roadmap "全部 Zipper.* 引 UniTask/VContainer" 的偏差已在 v0.4 记录 |
| 分发 | 目标仍是右键导出 .unitypackage；UniTask(UPM git)/VContainer(UPM) 无法随包 → **使用者决定暂缓**，现阶段设计不考虑分发 |

## 3. 当前状态

- **文档版本**：roadmap v0.7、Core 设计 v0.7、资源管理器设计 v0.3、命名规范 v0.3、Git 工作流 v1.0、协作分工 v1.0
- **代码**：自 09-06 未变——`Assets/Zipper/{Core(空),DI(已注册),Pool(ZObjectPool),Resources(+Asset 句柄三件套)}`
- **仓库**：`xrzxrzx/UnityZipper` 分支 main，工作区干净

## 4. 待办 / 下一步

- [ ] **Core 实现（使用者）**：日志系统（双 Sink / 编译期剥离 / 运行时级别）→ 事件总线（`IZEventBus` / `ZEventBus`）→ R3 桥（M2 引入 R3 时）
- [ ] 设计缺口：**Audio / UI 管理器设计稿**；场景资源生命周期；Addressables 工程设置要点（配置步骤，非代码）
- [ ] 工程治理：AGENTS.d/、C# 编码规范、测试（EditMode 单测）尚未开始
- [ ] 桥的异常边界：UI 层订阅链尾的统一兜底约定
- [ ] 分发决策（暂缓中）：回 vendor 还是列前置依赖
- [ ] roadmap 审批：v0.7 仍待使用者签署

## 5. 变更记录

| 日期 | 说明 |
|---|---|
| 2026-09-10 | 初版：记录 09-07~09-10 的文档治理（去实现化、新增两份规范）、事件总线五步收敛、分发暂缓、当前状态与待办 |
